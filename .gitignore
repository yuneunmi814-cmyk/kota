# 의존성
node_modules/

# 환경변수·시크릿 (절대 커밋 금지)
.env
.env.local
.env.*.local
*.pem

# 사업자/개인 PII — 공개 레포에 커밋 금지 (앱 내 표시는 별도 설정으로)
*.local.md
**/사업자정보.local.*

# TourAPI 원본 사진 — 저작권(한국관광공사). 미리보기 PNG에는 출처표기와 함께 임베드, 원본은 비커밋
design/screens/photos/

# 빌드 산출물
dist/
build/
.expo/
*.tsbuildinfo

# 로컬 도구·OS
.claude/
.DS_Store
*.log
